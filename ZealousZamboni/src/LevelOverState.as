package 
{
	import org.flixel.FlxState;
	
	/**
	 * ...
	 * @author Kenny
	 * 
	 * This state is the menu that the player sees in between levels, either after losing or winning. It is not the same
	 * as the menu that the player sees upon starting up the game
	 */
	public class LevelOverState extends FlxState 
	{
		
	}
	
}